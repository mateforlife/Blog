<%= form_for([@article,@comment], remote: true, html: { id:"comments-form "}) do |form| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.article.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_area :body, id: :comment_body, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Comentar', class: 'btn', id: 'button-new' %>
  </div>
<% end %>
